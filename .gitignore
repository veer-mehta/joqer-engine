# Secrets
.env

# BC pipeline — generated at runtime, never commit
dataset.jsonl        
# collected gameplay data (can be huge)
model.pt             
# trained weights
model_config.json    
# saved with model.pt
training_log.json    
# per-epoch metrics

# Intermediate game files
state.json
decision.json

# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual environment
apenv/
venv/
.venv/